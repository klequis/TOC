// Navigation
import React from 'react';

const Navigation = () => (
  // not finishing this now. Likely that havigation should be within MenuItems and MenuItems should be called Navigation. The TOC can hide when you are inside of a lesson.
);

export default Navigation;
